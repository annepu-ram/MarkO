# Hero Card Variant: The Lead Gen
# layout-row featuring a value prop on the left and a form on the right

- name: page
  properties:
    appearance:
      background: { color: '#f0f4f8' }
  components:
    - name: layout-row
      properties:
        layout: { horizontalAlign: space-evenly, verticalAlign: center, wrap: nowrap, gap: xl }
        spacing: { paddingBlock: xxl, paddingInline: xl }
        appearance:
          background: { color: '#ffffff' }
          radius: lg
          shadow: elevated
      components:
        - name: layout-column
          properties:
            layout: { horizontalAlign: left, widthMode: "50", gap: sm }
            appearance: { background: { transparency: 0 } }
          components:
            - name: eyebrow
              properties:
                text: START YOUR FREE TRIAL
                typography: { size: xs, weight: semibold, color: '#059669', transform: uppercase, letterSpacing: wider }
            - name: heading
              properties:
                text: Grow Your Business 10x Faster
                level: 1
                typography: { size: xxl, weight: extrabold, color: '#111827', align: left, letterSpacing: tight }
                spacing: { marginBlock: sm }
            - name: paragraph
              properties:
                text: |
                  Join 50,000+ businesses that trust us to drive growth.
                  No credit card required. Cancel anytime.
                typography: { size: md, color: '#4b5563', align: left, lineHeight: "1.7" }
            - name: layout-row
              properties:
                layout: { horizontalAlign: left, verticalAlign: center, gap: md, wrap: wrap }
                appearance: { background: { transparency: 0 } }
              components:
                - name: caption
                  properties:
                    text: "Trusted by 500+ companies"
                    typography: { size: xs, color: '#9ca3af', align: left }
        - name: layout-column
          properties:
            layout: { horizontalAlign: center, widthMode: "50", gap: sm }
            appearance:
              background: { color: '#f9fafb' }
              radius: lg
              padding: { block: lg, inline: lg }
              border: { width: 1, style: solid, color: '#e5e7eb' }
          components:
            - name: heading
              properties:
                text: Get Started Today
                level: 3
                typography: { size: lg, weight: bold, color: '#111827', align: center }
                spacing: { marginBlock: sm }
            - name: form
              properties:
                layout: { direction: column }
                submit: { show: false }
              components:
                - name: textbox
                  properties:
                    label: { text: Full Name, show: true }
                    field: { placeholder: 'John Doe', required: true }
                    appearance: { border: { width: 1, color: '#d1d5db' }, radius: sm }
                    layout: { widthMode: stretch }
                - name: textbox
                  properties:
                    label: { text: Work Email, show: true }
                    field: { placeholder: 'john@company.com', required: true }
                    appearance: { border: { width: 1, color: '#d1d5db' }, radius: sm }
                    layout: { widthMode: stretch }
                - name: button
                  properties:
                    text: Start Free Trial
                    appearance: { background: { color: '#059669' }, radius: md }
                    typography: { color: '#ffffff', weight: bold }
                    layout: { widthMode: stretch }
