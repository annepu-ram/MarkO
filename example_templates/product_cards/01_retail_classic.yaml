# Product Card Variant: Retail Classic
# layout-column with image, heading, caption (Price), and button

- name: page
  properties:
    appearance:
      background: { color: '#f9fafb' }
  components:
    - name: layout-column
      properties:
        layout: { horizontalAlign: center, gap: sm }
        appearance:
          background: { color: '#ffffff' }
          radius: lg
          shadow: soft
          border: { width: 1, style: solid, color: '#e5e7eb' }
          padding: { block: none, inline: none }
        spacing: { marginBlock: lg, marginInline: lg }
      components:
        - name: image
          properties:
            source: { url: 'https://images.unsplash.com/photo-1523275335684-37898b6baf30?w=600', altText: 'Premium Watch' }
            appearance: { minHeight: 18, fit: cover, cornerStyle: none }
            layout: { widthMode: stretch }
            spacing: { marginBlock: none }
        - name: layout-column
          properties:
            layout: { horizontalAlign: left, gap: xs }
            appearance: { background: { transparency: 0 }, padding: { block: md, inline: md } }
          components:
            - name: eyebrow
              properties:
                text: NEW ARRIVAL
                typography: { size: xxs, weight: semibold, color: '#059669', transform: uppercase, letterSpacing: wider }
            - name: heading
              properties:
                text: Classic Timepiece
                level: 3
                typography: { size: lg, weight: bold, color: '#111827', align: left }
                spacing: { marginBlock: xs }
            - name: caption
              properties:
                text: "$299.00"
                typography: { size: lg, weight: bold, color: '#111827', align: left }
            - name: button
              properties:
                text: Add to Cart
                appearance: { background: { color: '#111827' }, radius: md }
                typography: { color: '#ffffff', weight: semibold }
                layout: { widthMode: stretch }
