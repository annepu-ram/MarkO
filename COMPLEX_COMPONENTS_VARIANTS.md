1. Hero Card Variants (The First Impression)
The Standard Split: layout-row with layout-column (Text) and image.

Immersive Motion: video-background containing a centered layout-column with heading and button.

The Lead Gen: layout-row featuring a value prop on the left and a form (with textbox and button) on the right.

The Tabbed Explorer: layout-column with a tabs component to switch between different service categories.

Centered Minimalist: layout-column (align: center) with an eyebrow, heading, and paragraph.

The Story Starter: carousel containing three slides, each with a different image and heading overlay.

Search Hero: layout-column centered over a video-background with a large textbox for site-wide search.

Product Launch: layout-row with a gif of the product and a layout-column containing a calendar input for "Book a Demo."

Social Proof Hero: layout-column with a primary heading, followed by a layout-row of small partner image components.

The Navigation Hero: layout-column that starts with a titlebar, followed by a large blockquote and a primary button.

2. Product Card Variants (The Conversion Drivers)
Retail Classic: layout-column with image, heading, caption (Price), and button.

The Featurette: layout-column with a gif thumbnail, heading, and an accordion for technical specs.

The Bundle: layout-column using a columnsgrid to show three small items included in the package.

Limited Drop: layout-column with an eyebrow (Sale tag), image, and a caption showing stock remaining.

The Configurator: layout-column with image and a form containing a dropdown for size and radio for color.

Comparison Card: layout-column with a checkbox at the top for "Compare," followed by image and paragraph.

Video Demo: layout-column with a nested video (muted loop) and a "Buy Now" button.

The Quick-Add: layout-row (horizontal card) with a small image on the left and heading + button on the right.

Subscription Card: layout-column with tabs to toggle between Monthly/Yearly pricing before the button.

Review-Integrated: layout-column with image, heading, and a layout-row showing star icons and a link to reviews.

3. Review & Testimonial Cards (The Trust Builders)
The CEO Quote: layout-column with a large blockquote, caption (Name/Title), and a small circular image.

The Video Shoutout: layout-column with an embedded video testimonial and a paragraph transcript below.

The Grid Wall: A columnsgrid containing multiple small layout-column cards with just paragraph and eyebrow.

Social Post Style: layout-row with an image (avatar) and a layout-column containing heading (Username) and paragraph.

Before & After: layout-row with two image components and a caption underneath each.

The Star Rating: layout-column starting with a layout-row of star icons, followed by a heading and paragraph.

The Logo Bar: A simple layout-row of grayscale image components (client logos) with a caption "Trusted by 500+ companies."

Interactive Feedback: layout-column with a blockquote and an accordion labeled "Read full case study."

The Detailed Review: layout-column with a heading, paragraph, br, and a link to the verified purchase.

Carousel Praise: A carousel where each slide is a high-impact video-background with a blockquote overlay.

4. Story & Blog Cards (The Engagement Hooks)
Editorial Card: layout-column with a full-bleed image, eyebrow (Category), heading, and paragraph (Excerpt).

The Listicle: layout-row with a large number (using heading) on the left and layout-column content on the right.

Horizontal Story: layout-row (60/40 split) with image on the left and layout-column with heading and link on the right.

The Interactive Story: layout-column with a gif header and an accordion that reveals "Key Takeaways."

The Podcast Card: layout-row with an image (Album art), heading, and a button (Play icon).

Category Card: layout-column with a blurred image background and a centered heading (e.g., "Architecture").

The Newsletter In-Feed: layout-column with a heading, paragraph, and a form with a textbox for email signup.

The Timed Read: layout-column with image, heading, and a caption saying "5 min read • By Admin."

The Floating Header: layout-column where the image has a nested layout-column overlay containing the eyebrow and heading.

The Series Card: layout-column with a heading and a tabs component to navigate between "Part 1, Part 2, Part 3."
5. Pricing & Plan Cards (The Value Proposition)
These focus on transparency and conversion.

The Tiered Comparison: layout-column containing heading (Plan Name), caption (Price), br, and a list of paragraph items with checkbox icons.

Toggle Switcher: layout-column with a centered tabs (Monthly/Annual) that updates the heading (Price) via the parent container.

The "Most Popular" Ribbon: layout-column with a nested eyebrow at the very top (absolute positioned), followed by heading and button.

Feature Grid Card: columnsgrid (3 cols) where each cell is a layout-column showing a feature image and paragraph.

The Minimalist Link: layout-row with heading (Plan Name) and link ("See all features") leading to a modal.

The Enterprise Inquiry: layout-column with heading ("Custom"), paragraph ("For 100+ users"), and a button that opens a form.

The Sliding Scale: layout-column with a textbox (number input) that dynamically changes the caption (Price) text.

The Value Stack: layout-column where each line is a layout-row containing a "Checkmark" image and a paragraph.

Benefit Summary: layout-column with an image (Icon), heading, and a link to "Learn more about this tier."

The Sticky Footer Plan: layout-row pinned to the bottom of the page containing a heading and a "Sign Up" button.

6. Dashboard & Data Cards (The Utility Hub)
Widely used for SaaS platforms, admin panels, and user profiles.

The Stat Snapshot: layout-column with a caption (Metric Title) and a large heading (The Number, e.g., "$12.4k").

Trend Indicator: layout-row containing a heading (Value) and a green eyebrow ("+12%").

The Activity Feed: layout-column with multiple layout-row items, each containing a small image (avatar) and paragraph (action).

Task Accordion: accordion where each section is a "Task Name" and the expanded content is a textarea for notes.

The Progress Card: layout-column with heading, paragraph, and a custom-styled br (acting as a progress bar).

Quick Action Bar: layout-row with three button components (e.g., "Add User," "Export," "Print").

Data Filter Card: layout-column containing a dropdown, a calendar, and a "Search" textbox.

The Profile Header: layout-row (align: center) with a large image (avatar), layout-column (Name/Role), and a button (Edit).

Notification Card: layout-row with a "Bell" image and a paragraph that can be dismissed.

Chart Placeholder: layout-column with a video-background (muted animation of data) and a caption describing the trend.

7. Navigation & Footer Variants (The Framework)
The Mega-Menu: titlebar that, when hovered, reveals a columnsgrid containing links and images.

Sticky Titlebar: titlebar pinned to the top with a hamburger menu for mobile viewports.

Social Footer: layout-row (justify: space-between) with a layout-column (Logo/Copyright) and a row of link icons.

The Newsletter Footer: layout-row with a paragraph on the left and a form (email/button) on the right.

Multi-Column Footer: columnsgrid (4 cols) where each column is a layout-column of link items under a heading.

The Breadcrumb: layout-row of link components separated by / characters.

The Mobile Bottom-Bar: layout-row pinned to the screen bottom with four centered button or image icons.

Search Bar Header: titlebar that includes a textbox directly in the center for quick navigation.

The Tabbed Nav: tabs component used as the primary site navigation across the top of the page.

The Language Switcher: layout-row in the titlebar containing a dropdown and a "Login" button.

8. Portfolio & Showcase Cards (The Creative Set)
The Hover Overlay: image component with a nested layout-column (opacity: 0) that appears on hover, showing a heading.

The Video Teaser: layout-column where the header is a video (no controls) and the footer is a link to the full case study.

Masonry Style: columnsgrid (varying heights) where each card is a simple image and caption.

The Client Logo Slide: carousel that moves through a layout-row of brand image components.

The Split-Screen Project: layout-row with 50% image and 50% blockquote (Client quote).

The Tech Stack Card: layout-column with heading and a layout-row of small image icons representing software.

The Result Card: layout-column with a heading ("200% Growth") followed by a paragraph explaining the project impact.

The Interactive Gallery: tabs where each tab shows a different columnsgrid of images.

The "Meet the Team": layout-column with an image, heading, and a link to their LinkedIn.

The Animated Process: layout-row using gif icons to explain a 3-step workflow (Step 1, 2, 3).